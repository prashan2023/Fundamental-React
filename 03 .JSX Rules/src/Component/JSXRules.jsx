import React from 'react'

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>JSX must return a single parents element.</p>
      <p>JSX elements must be property closed.</p>
      <p>JSX attributes are written using camalCase (e.g. className instead of class & htmlFor instead of for) </p>
    </div>
  )
}

export default JSXRules
